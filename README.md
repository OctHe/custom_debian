# custom_debian

Custom Debian distribution based on live-build.


    sudo apt install live-build
    mkdir custom_debian
    cd custom_debian
    lb config   # Change the mirror
    sudo lb build
    lb clean
